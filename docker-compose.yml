services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: facechain_backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
    restart: unless-stopped